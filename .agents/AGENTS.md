# Agent Rules

- **Do NOT automatically push commits to the remote repository.** The user prefers to review all commits and push them manually. You may stage and commit changes (`git add`, `git commit`), but never execute `git push` unless explicitly asked.
