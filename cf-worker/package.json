{
  "name": "om-pdf-drive-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev":    "wrangler dev",
    "deploy": "wrangler deploy",
    "kv:create": "wrangler kv:namespace create DRIVE_TOKENS",
    "secret:set": "node scripts/set-secrets.js",
    "gen-key": "node -e \"console.log(require('crypto').randomBytes(32).toString('hex'))\""
  },
  "devDependencies": {
    "wrangler": "^3.57.0"
  }
}
